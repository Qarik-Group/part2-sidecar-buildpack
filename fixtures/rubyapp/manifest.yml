applications:
- name: app-using-config-server
  instances: 1
  memory: 256M
  disk_quota: 512M
  env:
    CONFIG_SERVER_PORT: 8082
  stack: cflinuxfs3
  buildpacks:
  - ruby_buildpack
  - https://github.com/drnic/sample-config-server-buildpack
  # sidecars:
  # - name: config-server
  #   process_types:
  #   - web
  #   command: './config-server'
